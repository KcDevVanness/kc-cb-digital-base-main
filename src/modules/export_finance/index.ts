import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'export_finance',
  title: 'Export Finance',
  version: '0.1.0',
  description: 'Collection records per purchase order and export tax-refund records per container, with the read-only order and container file projections.',
  author: 'App Team',
  license: 'MIT',
}
